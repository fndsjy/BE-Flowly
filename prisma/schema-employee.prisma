// schema-employee.prisma
datasource employee {
  provider = "sqlserver"
  url      = env("EMPLOYEE_DATABASE_URL")
}

generator employeeClient {
  provider = "prisma-client-js"
  output   = "../dist/generated/employee"
}
