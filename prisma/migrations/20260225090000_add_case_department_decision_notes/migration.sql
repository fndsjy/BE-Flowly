BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[case_department] ADD [decisionNotes] NVARCHAR(500);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
