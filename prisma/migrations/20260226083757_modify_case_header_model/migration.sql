BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[case_header] ADD [visibility] NVARCHAR(20) NOT NULL CONSTRAINT [case_header_visibility_df] DEFAULT 'PRIVATE';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
